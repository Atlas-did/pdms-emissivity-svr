# 交接总计划（30轮对话固化版）

> 目标：把“关键结论、当前状态矩阵、后续执行顺序”固定在本文件，新窗口可直接接续。

---

## 1. 关键结论（最终口径）

1. P1-1 产物统一口径：**以 share 包内重跑结果为准**，不再引用历史根目录产物。
2. P1-1 专用入口已收敛到 [stage2/run_p1_1.py](stage2/run_p1_1.py)。
3. P1-1 产物统一落盘到 [stage2/figures](stage2/figures)。
4. 图号与文件名采用 **Option A**：`figure_p1_*` 作为对外别名；现有 `figure15_*` 保持兼容。
5. 新增论文可引用索引表：[stage2/figures/paper_citation_table_p1_1.md](stage2/figures/paper_citation_table_p1_1.md)。

---

## 2. 当前状态矩阵（已完成/部分/未开始）

| 项目 | 状态 | 当前结果 | 下一步 |
|---|---|---|---|
| 元数据可追溯（run_id/schema/data_version） | 已完成 | Stage1 已注入并可导出 | 持续沿用 |
| 配置校验与性能埋点 | 已完成 | normalize/validate 与性能摘要已接入 | 持续沿用 |
| P1-1 执行入口收敛 | 已完成 | 新增 [stage2/run_p1_1.py](stage2/run_p1_1.py) | 用该入口重跑 |
| P1-1 输出目录收敛 | 已完成 | 图像默认目录改为 [stage2/figures](stage2/figures) | 禁止再写根目录 figures |
| P1-1 四联图 | 部分完成 | 已有 figure15 逻辑，可直接生成 | 以新入口重跑并归档 run_id |
| P1-1 统计表（全局+分区） | 部分完成 | 已新增 `direct_validation_summary.json/csv` 落盘 | 重跑后固化实值 |
| P1-2 物理约束图与统计 | 部分完成 | 已接入 `physical_constraint_summary.json/csv` 自动落盘 | 补齐数据后重跑并固化 run_id |
| P1-3 多区域消融完整表 | 部分完成 | 已接入 `ablation_summary.json/csv` 自动落盘 | 补齐数据后重跑并固化 run_id |
| P2（外推/不确定性） | 未开始 | 有实验骨架 | 按清单补跑 |
| P3（实验替换/温度讨论） | 未开始 | 文档级计划 | 论文定稿前补齐 |

---

## 3. 后续执行顺序（新窗口直接照做）

1. **P1-1**：运行 [stage2/run_p1_1.py](stage2/run_p1_1.py)
   - 预期输出：
     - [stage2/figures/figure15_direct_validation.png](stage2/figures/figure15_direct_validation.png)
     - [stage2/figures/direct_validation_summary.json](stage2/figures/direct_validation_summary.json)
     - [stage2/figures/direct_validation_summary.csv](stage2/figures/direct_validation_summary.csv)
2. **P1-2**：补充物理约束摘要导出（JSON/CSV + 图）
3. **P1-3**：补齐消融三策略统一对照表
4. 更新论文可引用索引表：[stage2/figures/paper_citation_table_p1_1.md](stage2/figures/paper_citation_table_p1_1.md)

---

## 4. 验收阈值与解释口径

1. P1-1 目标阈值：$MAE < 0.01$ 且 $R^2 > 0.98$。
2. 若局部分区出现负 $R^2$，需同步给出 `std(y)` 与 `MAE/RMSE` 解释，不视为自动失败。
3. 所有图表与表格必须可回溯到同一 `run_id`。

---

## 5. 交接检查清单

- [x] 交接矩阵已补全
- [x] P1-1 新入口已固定到 stage2
- [x] P1-1 默认输出目录已固定到 stage2/figures
- [x] 论文可引用表模板已创建
- [ ] 用新入口重跑一版并填实值

