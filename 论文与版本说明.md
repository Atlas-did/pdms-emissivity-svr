# 论文与版本说明（30轮对话交接版）

> 用途：集中记录“关键争议 → 最终口径”，避免新窗口丢上下文。

---

## 1. 关键争议与最终口径

### 争议 A：P1-1 产物沿用历史结果还是重跑

- 最终口径：**在 share 包内重跑一版**。
- 原因：避免引用工作区根目录历史 `figures`，确保路径与 run_id 一致。

### 争议 B：图号与文件名如何统一

- 最终口径：采用 **Option A**（`figure_p1_*` 作为论文映射别名）。
- 兼容策略：现有代码输出 `figure15_direct_validation.png` 保留；论文表内可映射为 `figure_p1_1_direct_validation`。

### 争议 C：是否需要“论文可直接引用表”

- 最终口径：**需要**。
- 文件： [stage2/figures/paper_citation_table_p1_1.md](stage2/figures/paper_citation_table_p1_1.md)
- 表字段：图号、`run_id`、输入数据、输出文件、阈值结论。

---

## 2. 本轮实际落地变更（代码与路径）

1. Stage2 默认图像目录改为 [stage2/figures](stage2/figures)（不再写根目录 `figures`）。
2. 新增 P1-1 专用入口 [stage2/run_p1_1.py](stage2/run_p1_1.py)。
3. P1-1 统计结果新增落盘：
	- [stage2/figures/direct_validation_summary.json](stage2/figures/direct_validation_summary.json)
	- [stage2/figures/direct_validation_summary.csv](stage2/figures/direct_validation_summary.csv)

---

## 3. 论文引用时的统一描述模板

可直接使用：

- “P1-1 直接验证在统一 `run_id` 下执行，四联图与统计表均由 Stage2 同目录产出。”
- “验收阈值为 $MAE<0.01$ 且 $R^2>0.98$，并同步报告分区级别统计。”

---

## 4. 下一轮续改锚点

1. 先跑 P1-1 并回填引用表 `run_id`。
2. 再做 P1-2（物理约束）摘要导出。
3. 最后做 P1-3（消融三策略）统一表格。
